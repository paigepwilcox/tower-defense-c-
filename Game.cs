namespace TowerDefenseGame
{
    class Game
    {
        public void Main()
        {
            // Instatiation - an instance of a class. creates a new variable tower with the type Tower, assigning to a new Tower() object. 
            Tower tower = new Tower();
        }
    }
}